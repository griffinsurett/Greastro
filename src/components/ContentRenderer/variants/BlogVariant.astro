---
// src/components/section/variants/BlogVariant.astro
/**
 * Blog Variant - Article/Blog Post Layout
 *
 * Optimized for blog content with:
 * - ArticleCard components showing author, date, tags
 * - 1-3 column responsive layouts
 * - Gradient CTA button
 * - Centered header
 *
 * Best for blog posts, news articles, content with metadata.
 */

import type { BaseVariantProps } from "../ContentRenderer.types";
import ArticleCard from "@/components/LoopComponents/ArticleCard.astro";
import Button from "@/components/Button/Button";

interface Props extends BaseVariantProps {
  columns?: 1 | 2 | 3; // Fewer column options than grid (blog posts need more space)
}

const {
  items = [],
  title,
  description,
  className = "",
  columns = 2,
  collectionUrl,
  collectionTitle,
} = Astro.props as Props;

const viewAllText = `View All ${collectionTitle || "Articles"}`;

// Responsive column classes for blog layout
const columnClasses = {
  1: "grid-cols-1",
  2: "grid-cols-1 lg:grid-cols-2",
  3: "grid-cols-1 md:grid-cols-2 lg:grid-cols-3",
};
---

<section class={`py-16 ${className}`}>
  {/* Centered header */}
  {
    (title || description) && (
      <div class="text-center mb-12">
        {title && <h2 class="text-4xl font-bold mb-4">{title}</h2>}
        {description && (
          <p class="text-lg text-gray-600 max-w-2xl mx-auto">{description}</p>
        )}
      </div>
    )
  }

  {/* Grid of article cards */}
  <ul class={`grid ${columnClasses[columns]} gap-8 list-none`}>
    {
      items.map((item) => (
        <li>
          <ArticleCard {...item} />
        </li>
      ))
    }
  </ul>

  {/* Gradient CTA button */}
  {
    collectionUrl && items.length > 0 && (
      <div class="mt-12 text-center">
        <Button
          href={collectionUrl}
          rightIcon="lu:arrow-right"
          variant="primary"
          size="lg"
          className="rounded-full bg-gradient-to-r from-blue-600 to-blue-700 hover:from-blue-700 hover:to-blue-800"
        >
          {viewAllText}
        </Button>
      </div>
    )
  }
</section>
