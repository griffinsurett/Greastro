---
// src/components/section/loops/FeatureCard.astro
export interface Props {
  title: string;
  url?: string;
  description?: string;
  image?: {
    src: string;
    alt: string;
  };
  ctaText?: string;
}

const { title, url, description, image, ctaText = "Learn More →" } = Astro.props;
---

<div class="bg-white rounded-xl overflow-hidden shadow-lg hover:-translate-y-2 hover:shadow-xl transition-all duration-300 flex flex-col">
  {image && (
    <img 
      src={image.src} 
      alt={image.alt}
      class="w-full h-48 object-cover"
    />
  )}
  <div class="p-6 flex-1 flex flex-col">
    <h3 class="text-2xl font-bold mb-4">{title}</h3>
    {description && (
      <p class="text-gray-600 mb-6 flex-1 leading-relaxed">{description}</p>
    )}
    {url && (
      <a 
        href={url} 
        class="text-blue-600 font-semibold hover:underline self-start"
      >
        {ctaText}
      </a>
    )}
  </div>
</div>